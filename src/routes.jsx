import React from 'react';
import {
    BrowserRouter
} from 'react-router-dom';
import Container from './components/container/index'

export default () => (
    <BrowserRouter>
        <Container/>
    </BrowserRouter>
)